{
  "name": "storyboard-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ushka.github.io/react-tutorial/",
  "devDependencies": {
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "gh-pages": "^1.0.0",
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "predeploy": "yarn run build",
    "deploy": "gh-pages -o react-tutorial -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
